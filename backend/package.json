{
  "name": "@trafficpoppy/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrafficPoppy backend sidecar — deploys and tears down the analytics stack via AgentsPoppy-brokered credentials.",
  "main": "src/server.ts",
  "scripts": {
    "pretypecheck": "npm run gen:backend --prefix ..",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run gen:backend --prefix ..",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-lambda": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
