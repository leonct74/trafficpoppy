{
  "name": "@trafficpoppy/lambdas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrafficPoppy collector Lambda — serves t.js and ingests privacy-first pageview events.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
