{
  "name": "@trafficpoppy/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrafficPoppy's CloudFormation template, authored as typed TypeScript (no cdk).",
  "main": "src/template.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
